services:
  tv-sleep-api:
    build: .
    container_name: tv-sleep-api
    user: "1000:1000"
    environment:
      - TZ=Europe/Rome
      - TV_SLEEP_DB=/data/tv_sleep.db
      - TV_SLEEP_PORT=8010
    ports:
      - "8010:8010"
    volumes:
      - ./data:/data
    restart: unless-stopped
